# ========================
# SAFE MODE CONFIGURATION
# ========================
# Copy this to .env.local and update with your values

# Financial Compliance
CREDITS_ONLY=true                    # Internal ledger, not stored value
ENABLE_CRYPTO=false                  # Disable BTC/ETH (future feature)
TRUST_FEE_ENABLED=true               # Trust-based fee calculation (5-50%)
CONNECT_ENABLED=true                 # Stripe Connect payouts

# Stripe Configuration (Get from: https://dashboard.stripe.com/test/apikeys)
STRIPE_SECRET_KEY=sk_test_51...
STRIPE_PUBLISHABLE_KEY=pk_test_51...
STRIPE_WEBHOOK_SECRET=whsec_...

# App URL (Update for production)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# KYC/Verification
REQUIRE_INSTRUCTOR_KYC=true          # Must verify before payouts
MIN_PAYOUT_AMOUNT_CENTS=5000         # $50 minimum withdrawal

# Rate Limiting (Requires Upstash Redis)
UPSTASH_REDIS_REST_URL=https://...
UPSTASH_REDIS_REST_TOKEN=...
PURCHASE_RATE_LIMIT=10               # 10 purchases per hour
PAYOUT_RATE_LIMIT=3                  # 3 payouts per day

# Audit
AUDIT_ALL_TRANSACTIONS=true          # Log every money movement

# ========================
# QUICK START GUIDE
# ========================
# 1. cp .env.example.marketplace .env.local
# 2. Update Stripe keys (get from Stripe Dashboard)
# 3. Update NEXT_PUBLIC_APP_URL with your domain
# 4. npx prisma db push
# 5. npm run dev
# 6. Test purchase flow with Stripe test card: 4242 4242 4242 4242
# 
# See SAFE_MODE_RUNBOOK.md for complete deployment guide
