/**
 * Database Client Export
 *
 * Re-exports the Prisma client instance for use throughout the application.
 * This allows importing via `@/lib/db` instead of `@/lib/db/prisma`.
 */

export { prisma } from "./prisma";
